{
  "name": "tpwebsite",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "import:news": "node scripts/import-news.mjs",
    "extract:news-images": "node scripts/extract-news-images.mjs",
    "extract:assets": "node scripts/extract-assets.mjs",
    "migrate": "npm run import:news && npm run extract:news-images && npm run extract:assets"
  },
  "dependencies": {
    "@fontsource/inter-tight": "^5.2.7",
    "@fontsource/space-grotesk": "^5.2.10",
    "astro": "^5.6.0",
    "yaml": "^2.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
